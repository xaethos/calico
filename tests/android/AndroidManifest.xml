<?xml version="1.0" encoding="utf-8"?>
<!-- package name must be unique so suffix with "integration" so package loader doesn't ignore us -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.xaethos.lib.calico.integration"
    android:versionCode="3"
    android:versionName="0.2.1" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="16" />

    <application android:label="@string/app_name" >
        <uses-library android:name="android.test.runner" />
    </application>

    <instrumentation
        android:name="android.test.InstrumentationTestRunner"
        android:label="Tests for net.xaethos.app.calicosample"
        android:targetPackage="net.xaethos.app.calicosample" />

</manifest>