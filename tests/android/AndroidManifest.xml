<?xml version="1.0" encoding="utf-8"?>
<!-- package name must be unique so suffix with "integration" so package loader doesn't ignore us -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="net.xaethos.lib.calico.integration"
          android:versionCode="2"
          android:versionName="0.2">

    <application android:label="@string/app_name">
        <uses-library android:name="android.test.runner" />
        <activity android:name=".UsersActivity" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="vnd.android.cursor.dir/vnd.xaethos.test.user" />
            </intent-filter>
        </activity>

        <activity android:name=".EditUserActivity">
            <intent-filter android:label="New user">
                <action android:name="android.intent.action.INSERT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="vnd.android.cursor.dir/vnd.xaethos.test.user" />
            </intent-filter>
            <intent-filter android:label="Edit user">
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="vnd.android.cursor.item/vnd.xaethos.test.user" />
            </intent-filter>
        </activity>

        <provider android:name=".MyProvider" android:authorities="net.xaethos.lib.calico.integration" />
    </application>

    <instrumentation android:name="android.test.InstrumentationTestRunner"
                     android:targetPackage="net.xaethos.lib.calico.integration"
                     android:label="Tests for net.xaethos.lib.calico"/>
</manifest>
